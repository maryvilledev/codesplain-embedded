import React, { Component, PropTypes } from 'react';
import { connect } from 'react-redux';

class SnippetArea extends Component {
  render() {
    const { snippetKey } = this.props
    
  }
}

export connect()(SnippetArea);
